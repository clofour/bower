DATABASE_URL=postgres://canopy:canopy@localhost:5432/canopy
# Stable across every instance in a multi-instance deployment.
NEXT_SERVER_ACTIONS_ENCRYPTION_KEY=
# Override when publishing the bundled proxy images under another registry.
CANOPY_CADDY_IMAGE=ghcr.io/clofour/canopy-caddy:latest
CANOPY_PROXY_SYNC_IMAGE=ghcr.io/clofour/canopy-proxy-sync:latest
