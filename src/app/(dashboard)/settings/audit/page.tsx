import { requireContext } from '@/lib/actions/shared';import { getAuditLog } from '@/lib/queries';import { PageHeading } from '@/components/page-heading';import { DateText,Empty } from '@/components/ui'
export default async function Page(){const c=await requireContext(),rows=await getAuditLog(c.org.id);return <><PageHeading eyebrow="Governance" title="Audit log" description="A durable timeline of administrative and operational changes."/><div className="table-wrap">{rows.length?<table><thead><tr><th>Action</th><th>Actor</th><th>Resource</th><th>Time</th></tr></thead><tbody>{rows.map(({entry:e,userName})=><tr key={e.id}><td><strong>{e.action.replaceAll('.',' · ')}</strong></td><td>{userName||'System'}</td><td>{e.resourceType} <span className="mono">{e.resourceId.slice(0,8)}</span></td><td><DateText value={e.createdAt}/></td></tr>)}</tbody></table>:<Empty title="No recorded changes">Administrative and deployment actions will appear here.</Empty>}</div></>}
