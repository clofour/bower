import { notFound } from 'next/navigation';import { requireContext } from '@/lib/actions/shared';import { getProjectBySlug,getDeploymentsByProject } from '@/lib/queries';import { PageHeading } from '@/components/page-heading';import { DateText,Empty,Status } from '@/components/ui'
export default async function Page({params}:{params:Promise<{slug:string}>}){const {slug}=await params,c=await requireContext(),p=await getProjectBySlug(c.org.id,slug);if(!p)notFound();const rows=await getDeploymentsByProject(p.id);return <><PageHeading eyebrow="Delivery" title="Deployments" description="A chronological record of every release, promotion, and rollback."/><div className="table-wrap">{rows.length?<table><thead><tr><th>Service</th><th>Environment</th><th>Image</th><th>Trigger</th><th>Status</th><th>Started</th></tr></thead><tbody>{rows.map(({deployment:d,...r})=><tr key={d.id}><td><strong>{r.serviceName}</strong></td><td>{r.environmentName}</td><td className="mono">{d.imageAfter}</td><td>{d.triggerType}</td><td><Status value={d.status}/></td><td><DateText value={d.startedAt}/></td></tr>)}</tbody></table>:<Empty title="Nothing has shipped yet">Deploy a service and its progress will appear here.</Empty>}</div></>}
