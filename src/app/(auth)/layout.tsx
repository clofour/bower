import { Brand } from '@/components/brand'
export default function AuthLayout({children}:{children:React.ReactNode}){return <main className="auth"><section className="auth-story"><Brand light/><div className="auth-copy"><div className="eyebrow" style={{color:'#8fc4a5'}}>Trellis, carefully tended</div><h1>Grow services without losing the plot.</h1><p>Bower keeps environments, deployments, routes, and secrets in one calm operational workspace.</p></div><div className="orbit"><span className="seed" /></div><div className="trail"/><small style={{color:'#819b8d',position:'relative'}}>A control plane for deliberate infrastructure.</small></section><section className="auth-form">{children}</section></main>}
